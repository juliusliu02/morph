import React from "react";

const prompt = () => {
  return (
    <section className="relative top-[calc(-100vh+20rem)] h-96">
      <h2 className="text-2xl text-slate-900 dark:text-slate-50 sm:text-3xl font-semibold text-center sticky top-4 sm:top-12">
        Get creative with your own prompts.
      </h2>
    </section>
  );
};

export default prompt;
